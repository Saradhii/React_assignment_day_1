import React from "react";

const Settings = (props)=>
{
    console.log(props);
    return (
        <>
          <h3>{props.header}</h3>
        </>
    );
};


export default Settings;